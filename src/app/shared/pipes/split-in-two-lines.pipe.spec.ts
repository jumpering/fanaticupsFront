import { SplitInTwoLinesPipe } from './split-in-two-lines.pipe';

describe('SplitInTwoLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitInTwoLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
